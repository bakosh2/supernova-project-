The revised implementation plan is approved with the following final amendments.

Keep the code simple, readable, and student-friendly as already specified. Do not introduce unnecessary architecture.

## 1. TaskCreationView integration is correct

The TaskCreationView values should map as planned:

- taskTitle → HomeworkTask.title
- focusDuration.rawValue × 60 → focusDurationSeconds
- breakDuration.rawValue × 60 → breakDurationSeconds
- steps.map(\.text) → ordered HomeworkSubtask objects
- validity → validUntil and optionally validityRawValue
- requirePin → requiresCompletionPIN

`newStepText` is temporary input only and must not be persisted.

The current TaskCreationView has no subject field, so `subject` must remain optional or have an empty default.

## 2. Give the subtask relationship a default value

Use:

```swift
@Relationship(
    deleteRule: .cascade,
    inverse: \HomeworkSubtask.task
)
var subtasks: [HomeworkSubtask] = []
Do not leave the relationship uninitialized.
When the helper creates subtasks, assign their parent task relationship correctly.
3. Remove unused break pause state unless required
The current product requirement allows:
Focus pause and resume
Break skipping
It does not require pausing a break.
To keep the code simple, remove these unless an existing requirement genuinely uses them:
case breakPaused
var isBreakPaused: Bool
The phase enum can be:
enum TaskSessionPhase: String, Codable {
    case notStarted
    case focus
    case focusPaused
    case breakTime
    case completed
}
Do not keep unused states that make the implementation harder to understand.
4. HomeworkTask must remain the persistent source of truth
TaskSessionViewModel may expose @Published values for the interface, but it must initialize them from the supplied HomeworkTask.
Every meaningful update must also be written back to HomeworkTask immediately.
This includes:
phase
currentSubtaskIndex
focusSecondsRemaining
breakSecondsRemaining
focus pause state
subtask completion
totalFocusSecondsCompleted
task completion
Avoid two independent copies of the same state becoming out of sync.
5. Define first-entry behavior
When opening a new task with phase .notStarted:
Set currentSubtaskIndex to 0
Set focusSecondsRemaining to task.focusDurationSeconds
Set breakSecondsRemaining to task.breakDurationSeconds
Change phase to .focus
Start the focus timer
Keep all subtasks unchecked
Do not reset a task that has already been started.
6. Define resume behavior clearly
When reopening a previously started task:
Restore its current subtask
Restore all completed checkboxes
Restore its saved phase
Restore exact focus or break seconds
Do not subtract time spent outside the page
Do not automatically continue the timer
For a saved focus session:
Restore it as paused
Display the focus screen
Require the child to press “متابعة”
For a saved break session:
Restore the break screen and exact remaining value
The break should only count down while TaskSessionView is active
The timer must never continue running while the child is on another page.
7. TaskSessionView API
TaskSessionView must accept:
TaskSessionView(
    task: HomeworkTask,
    onExit: @escaping () -> Void,
    onTaskCompleted: @escaping (UUID) -> Void
)
TaskSessionViewModel may also receive the completion callback if needed.
The same HomeworkTask object must later flow through:
Task List
→ Help Me Start
→ Task Session
Do not search for a task using its title.
8. Prepare Help Me Start integration
Update or prepare HelpMeStartView so it can later receive the selected task.
Use an API similar to:
HelpMeStartView(
    task: selectedTask,
    onBack: {
        // Return to task details
    },
    onStart: { task in
        // Open TaskSessionView with the same task
    }
)
Do not create a copy of HomeworkTask when the child presses “يلا نبدأ!”.
If changing the current HelpMeStart API would break existing previews, provide a temporary convenience initializer.
9. Leave confirmation behavior
The upper-right navigation button must display:
Title:
الخروج من المهمة؟
Message:
سيتم حفظ وقتك وتقدمك، ويمكنك المتابعة لاحقًا.
Actions:
البقاء
and:
الخروج وحفظ التقدم
When leaving is confirmed:
Stop and invalidate the timer
Save exact remaining seconds
Save current phase
Save currentSubtaskIndex
Save every subtask’s completion state
Save totalFocusSecondsCompleted
Update lastSavedAt
Save modelContext
Call onExit only after the save attempt
10. Scene phase and view disappearance
When scenePhase becomes .inactive or .background:
Stop the timer
Persist session state
Do not call onExit
When TaskSessionView disappears unexpectedly:
Stop the timer
Persist the current values
Make these methods safe to call more than once.
11. Guarantee only one timer
TaskSessionViewModel must store one timer reference.
Before starting a timer:
stopTimer()
Then create the new timer.
stopTimer() must invalidate the existing timer and set it to nil.
Do not create:
Multiple Timer instances
Multiple Combine subscriptions
Multiple async countdown loops
Only one countdown may be active.
12. Main action button rules
The main button must be disabled unless the current subtask is checked.
For a non-final subtask:
المهمة التالية
When tapped:
Keep the current step completed
Move to the next step
Save currentSubtaskIndex
Do not reset the focus timer
For the final subtask:
إنهاء المهمة
When tapped:
Stop the timer
Set isCompleted to true
Set phase to completed
Set completedAt to now
Save the model context
Call onTaskCompleted(task.id)
The button must remain disabled while the final subtask is unchecked.
13. Previous subtask rules
When currentSubtaskIndex is 0:
Previous button is gray
Previous button is disabled
When currentSubtaskIndex is greater than 0:
Previous button is purple
Previous button is enabled
Going backward must not uncheck or erase any saved subtask state.
14. Dynamic progress segments
The number of segments must equal:
task.sortedSubtasks.count
Use these visual states:
Completed previous subtask: purple
Current subtask: stronger purple
Future incomplete subtask: pale gray/lavender
Do not hard-code a segment count.
The design must also support 2 and 4 subtasks, not only the preview counts of 1, 3, and 5.
15. Focus and break cycle
The cycle remains independent of subtask navigation:
Focus reaches zero
→ switch to break
→ load full break duration
→ start break
Break reaches zero
→ switch to focus
→ load full focus duration
→ preserve current subtask and check states
→ start focus
Changing subtasks must not reset either timer.
Focus/break cycles may repeat indefinitely until the final subtask is completed.
16. Skip break
When the child skips the break:
Stop the current timer
Set phase to focus
Set focusSecondsRemaining to task.focusDurationSeconds
Preserve currentSubtaskIndex
Preserve all completion states
Save immediately
Begin the new focus timer
Do not mark the task completed.
17. Total focus time
Increment:
totalFocusSecondsCompleted
only when:
Phase is focus
Timer is actively running
Focus is not paused
Do not increment it during:
Break
Paused focus
Inactive/background state
Time spent outside TaskSessionView
18. SwiftData container
If SupernovaApp already has a model container, extend it.
Do not create a second container.
If no container exists, register:
.modelContainer(
    for: [
        HomeworkTask.self,
        HomeworkSubtask.self
    ]
)
All future pages must use this same container.
19. Asset verification
Before using:
Image("stretching_star")
confirm that stretching_star exists in Assets.xcassets.
If it is temporarily unavailable, keep the layout compilable in previews, but clearly report that the final illustration asset must be added.
Do not permanently replace it with an SF Symbol.
20. Build and simulator verification
After compile verification using:
xcodebuild \
  -project Supernova.xcodeproj \
  -scheme Supernova \
  -destination 'generic/platform=iOS Simulator' \
  clean build
also inspect available destinations and run the interface on an available landscape iPad simulator.
Visually compare:
Focus unchecked
Focus checked
First subtask
Middle subtask
Final subtask
Focus paused
Break session
21. Final simplicity review
Before finishing, simplify the implementation.
Remove:
Unused pause states
Unused properties
Duplicate state
Unnecessary protocols
Repository or service layers that are not needed
Complex dependency injection
Excessive extensions
Components that are only a few lines and used once
Long explanatory comments
Keep:
One clear SwiftData task model
One clear subtask model
One TaskSessionViewModel
One timer
Simple SwiftUI components
Straightforward functions
Brief useful comments
The code should be easy for a student developer to read, modify, and explain during a university project presentation.
Do not remove any required behavior in order to simplify the code.
Update the implementation plan one final time with these amendments, then begin implementation.

The most important correction is removing `breakPaused` and `isBreakPaused`. Since the child only pauses 